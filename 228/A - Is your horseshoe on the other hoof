#include <bits/stdc++.h>
#include <cctype>
using namespace std;
int main(){
    long long n;
    unordered_set<long long> s;
    for (int i=0;i<4;i++){
        cin >> n;
        s.insert(n);
    }
    cout << (4-s.size());
}